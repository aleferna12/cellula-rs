[package]
name = "evo-cpm"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.39", features = ["derive"] }
indexmap = "2.9.0"
rand = "0.9.1"
rand_xoshiro = "0.7.0"

[dev-dependencies]
criterion = { version = "0.6.0", features = ["html_reports"] }

[[bench]]
name = "main"
harness = false
